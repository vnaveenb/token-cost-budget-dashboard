[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "token_cost_dashboard"
version = "1.0.0"
requires-python = ">=3.10"

[tool.setuptools.package-dir]
token_cost_dashboard = "src"

[tool.setuptools]
packages = ["token_cost_dashboard"]
